<%= render_headline @headline %>
The following logmessages were generated by your ScopePort server installation. You can lookup error messages corresponding to error codes at <%= link_to "ScopePort.org", "http://scopeport.org/support/errorcodes" %>.

<div id="loglist">
	<table>
		<tr class="doNotHighlightMe">
			<th>Time</th>
			<th>Message</th>
			<th>Code</th>
			<th>Severity</th>
		</tr>
	<% @logmessages.each do |logmessage| %>
		<tr>
			<td><%=h Time.at logmessage.logtime %></td>
			<td class="bold"><%=h logmessage.logmsg %></td>
			<td>#<%=h logmessage.errorcode %></td>
			<td><%=h getNameOfErrorSeverity logmessage.severity %></td>
		</tr>
	<% end %>
	</table>
</div>

<div class="pagination">
	<%= will_paginate @posts %>
</div>

<div id="clearlog">
	<% form_tag :action => "clear" do %>
		<%= submit_tag "Clear log", :disable_with => "Please wait" %>
	<% end %>
</div>
