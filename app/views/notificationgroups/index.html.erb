<%= render_headline "Notification groups" %>
Notification groups are used as receiver for warnings. When you set the notification group of a sensor everybody in this group will be informed in case of an alarm.

<% @groups.each do |group| %>
		<ul>
		<li>
			<%= link_to group.name, :controller => "notificationgroups", :action => "show", :id => group.id %>
			(<%= showNumOfGroupReceivers @counts[group.id].to_i %>)
		</li>
		</ul>
<% end %>

<div id="new-notigroup">
	<h2>Add notification group</h2>
	<% form_for :newGroup, :url => { :action => "creategroup" } do |f| %>
		<dl>
			<dt><%= label :newGroup, :name, "Name of the new group" %></dt>
			<dd><%= f.text_field :name %> <%= error_message_on(:newGroup, :name) %></dd>
			<dt>&nbsp;</dt>
			<dd><%= submit_tag "Add group", :disable_with => "Please wait" %></dd>
		</dl>
	<% end %>
</div>
